randomise();
ultima_mensagem = noone; // Guarda a ID da última mensagem criada
distancia_segura = 160;   // A distância (em pixels) que uma deve estar da outra
alarm[0] = 60;

// Variáveis de Dificuldade (Opcional, como você pediu)
spawn_rate_base = 60;